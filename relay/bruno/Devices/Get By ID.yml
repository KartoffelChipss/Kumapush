info:
  name: Get By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/devices/:id"
  params:
    - name: id
      value: NyAAJ3yD4Q0dZC
      type: path
  headers:
    - name: Authorization
      value: Bearer {{auth-token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
